package sng.assem.enums;

// forward/reverse end of an EST
// or, whether a contig consists of forward/reverse EST, or a mix.
// Don't change these symbols as they are used in the DB. 
public enum RF {
	R, F, Unk, Mix
}
